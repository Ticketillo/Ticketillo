{
    "name": "ticketillo",
    "private": true,
    "scripts": {
        "start": "yarn docker:start:dev && lerna run start --stream",
        "bootstrap": "lerna bootstrap",
        "build": "lerna run build --stream",
        "clean": "lerna clean",
        "docker:start:dev": "docker-compose up -d"
    },
    "devDependencies": {
        "eslint": "^8.33.0",
        "lerna": "5.1.6",
        "prettier": "^2.8.3",
        "rimraf": "^3.0.2",
        "typescript": "^4.5.5"
    },
    "dependencies": {}
}
